#include <iostream>
#include <cstring>
#define maxn 1010
int a[maxn];
int main() {
    using namespace std;
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n, k, first = 1;
    memset(a, 0, sizeof(a));
    cin >> n >> k;
    
    return 0;
}